# Dev: Vite proxies /api → Laravel (see vite.config.mts VITE_DEV_API_TARGET)
VITE_API_URL=/api

# Production (Vercel): set at build time — must include /api suffix
# VITE_API_URL=https://your-api.onrender.com/api

# Google Sign-In — must match GOOGLE_CLIENT_ID in Back/.env
VITE_GOOGLE_CLIENT_ID=
